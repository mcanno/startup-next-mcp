{
  "name": "startup-next-mcp",
  "version": "0.1.0",
  "description": "Servidor MCP que expone el conocimiento de startup-next (Model Context Protocol, transporte stdio)",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.16.2",
    "@types/node": "^20.14.10"
  },
  "engines": {
    "node": ">=18"
  }
}
